@model IEnumerable<WorldForging.Models.Convention>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Subject.SubjectId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Subject.SubjectId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConventionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConventionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConventionId })
        </td>
    </tr>
}

</table>
